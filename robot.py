class Robot():
	def __init__(self,fichas,x,y,mapa,direccion):
		self.monedas=monedas
		self.y=0
		self.x=0
		self.fichas=0
		self.direccion=0
		self.mapa=caragr_mapa		
	def mover(self):
		if self.direccion==0 and self.x<80:
			self.x=1
		if self.direccion==0 and self.y>25:
			self.y=1
	def pick(self):
		for x in len(mapa.lista):
			self.fichas=fichas+1
		return fichas





<
>
v
^
